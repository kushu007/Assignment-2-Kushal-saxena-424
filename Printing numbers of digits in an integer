#include <stdio.h>

int main() 
{
  int Number, Reminder, count=0;

  printf(" Please Enter any number\n");
  scanf("%d", &Number);

  while(Number > 0)
  {
     Number = Number / 10;
         ++count;  
  }

  printf("\n Number of Digits in a Given Number = %d", count);
  return 0;
}
